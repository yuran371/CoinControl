spring:
  datasource:
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  security:
    user:
      name: admin
      password: 123
logging:
  level:
    root: info
    org.hibernate.type.descriptor.JdbcBindingLogging: trace
    org.hibernate.orm.jdbc.bind: trace


